package gameEngine;

import levels.Level;
import objects.Bird;

public class PhysicsEngine {

    private double gravity = 2;


    /*public void applyGravity(Bird bird) {
        if (bird.getIsLaunched()) {
           double forceY = gravity * bird.getMass();
           bird.applyForce(0, forceY);
        }
    }*/

    /*public void update(Bird bird) {
        applyGravity(bird);
    }*/
}
